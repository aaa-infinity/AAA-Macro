<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme: Modern Light Material3 Theme -->
    <style name="Theme.AAAMacro" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color (Emerald Green) -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <!-- Secondary brand color (Warm Amber) -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">#FFFFFF</item>
        <!-- Status bar and Window Background -->
        <item name="android:statusBarColor">@color/background_light</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:navigationBarColor">@color/background_light</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
        <item name="android:windowBackground">@color/background_light</item>
    </style>

    <!-- Translucent theme for Capture Activities -->
    <style name="Theme.AAAMacro.Translucent" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
